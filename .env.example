# URL of your existing backend (Express/TS) e.g. https://genius-backend.onrender.com
BACKEND_URL="http://localhost:4000"

# Base for public fetches in client
NEXT_PUBLIC_API_BASE="/api"

# Cookie name storing session token (issued by your backend)
SESSION_COOKIE_NAME="gg_session"
